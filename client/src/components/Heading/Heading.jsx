import React from "react";

const Heading = ({title}) => (
    <div className="app__heading" style={{ marginBottom: '70px' }}>
        <h2 className="headtext__pacifico">{title}</h2>
    </div>
);

export default Heading;